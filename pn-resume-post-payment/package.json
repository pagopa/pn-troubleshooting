{
  "name": "pn-resume-post-payment-sqs-feed",
  "version": "1.0.0",
  "private": true,
  "description": "Validates and publishes resume post-payment requests to SQS",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "nyc --check-coverage --lines 95 --statements 95 --functions 95 --branches 85 --reporter=text mocha 'test/**/*.test.js' --recursive --timeout=3000",
    "coverage": "nyc report --reporter=lcov"
  },
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "3.1124.0",
    "@aws-sdk/credential-provider-ini": "3.973.15",
    "csv-parse": "^6.1.0"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^9.2.2",
    "nyc": "^15.1.0",
    "proxyquire": "^2.1.3",
    "sinon": "^21.0.0"
  }
}
