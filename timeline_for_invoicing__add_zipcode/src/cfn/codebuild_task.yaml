AWSTemplateFormatVersion: '2010-09-09'
Description: CodeBuild for maven docker project
Parameters:
  ConfObjDumpS3Url:
    Description: Solo per uniformità nella struttura dei file yaml
    Type: String
  TimelineForInvoicingZipS3Url:
    Description: Solo per uniformità nella struttura dei file yaml
    Type: String
  ExportsBucket:
    Description: Solo per uniformità nella struttura dei file yaml
    Type: String
  CommitRef:
    Default: main
    Description: Solo per uniformità nella struttura dei file yaml
    Type: String
  TimelineForInvoicingRedonePath:
    Default: pnTimelineForInvoicing_with_zipCode_and_foreignState
    Description: Solo per uniformità nella struttura dei file yaml
    Type: String

Resources:
  CodeBuildProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: ComputeTimelineForInvoicingWithZipCodeAndForeignState
      ServiceRole: !GetAtt CodeBuildServiceRole.Arn
      BadgeEnabled: false
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/standard:6.0
        EnvironmentVariables:
          - Name: CONF_OBJ_DUMP_S3_URL
            Type: PLAINTEXT
            Value: !Sub "${ConfObjDumpS3Url}"
          - Name: TIMELINE_FOR_INVOICING_TGZ_S3_URL
            Type: PLAINTEXT
            Value: !Sub "${TimelineForInvoicingZipS3Url}"
          - Name: BUCKET_NAME
            Type: PLAINTEXT
            Value: !Sub "${ExportsBucket}"
          - Name: COMMIT_REF
            Type: PLAINTEXT
            Value: !Sub "${CommitRef}"
          - Name: S3_OUTPUT_PATH
            Type: PLAINTEXT
            Value: !Sub "${TimelineForInvoicingRedonePath}"
      Source:
        Type: NO_SOURCE
        BuildSpec: |
          version: 0.2
          env:
            shell: bash
          phases:
            pre_build:
              on-failure: ABORT
              commands:
                - env
                - echo ""
                - echo "DOWNLOAD DUMP"
                - mkdir -p input_data/conf_obj
                - aws s3 cp --recursive s3://${BUCKET_NAME}/${CONF_OBJ_DUMP_S3_URL} input_data/conf_obj
                - echo ""
                - echo "DOWNLOAD TIMELINE"
                - mkdir -p input_data/timeline
                - aws s3 cp s3://${BUCKET_NAME}/${TIMELINE_FOR_INVOICING_TGZ_S3_URL} input_data/timeline
                - (cd input_data/timeline && tar xvzf *.tgz && rm -f *.tgz)
                - echo ""
                - echo "DOWNLOAD SCRIPTS"
                - git clone https://github.com/pagopa/pn-troubleshooting.git
                - ( cd pn-troubleshooting && git checkout ${COMMIT_REF} )
                - echo ""
                - echo "LIST ALL FILES"
                - find . | sed -e "s/[^-][^\/]*\// |/g" -e "s/|\([^ ]\)/|-\1/"
            build:
              on-failure: ABORT
              commands:
                - echo ""
                - echo "RUN SCRIPTS"
                - pn-troubleshooting/timeline_for_invoicing__add_zipcode/run.sh
            post_build:
              on-failure: ABORT
              commands:
                - echo ""
                - echo "UPLOAD RESULT TO S3"
                - aws s3 cp out/pn-TimelineForInvoicing-redo-until-202309.tgz s3://${BUCKET_NAME}/${S3_OUTPUT_PATH}/pn-TimelineForInvoicing-redo-until-202309.tgz
      Artifacts:
        Type: NO_ARTIFACTS
      TimeoutInMinutes: 10
      
  CodeBuildServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: ''
            Effect: Allow
            Principal:
              Service:
                - codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: CodeBuildPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: CreateLogGroup
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                Resource:
                  - !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/ComputeTimelineForInvoicingWithZipCodeAndForeignState:log-stream:'
              - Sid: CreateAndWriteLogStream
                Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource:
                  - !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/ComputeTimelineForInvoicingWithZipCodeAndForeignState:log-stream:*'
              - Sid: ExportsBucket
                Effect: Allow
                Action:
                  - s3:GetReplicationConfiguration
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::${ExportsBucket}'
              - Sid: CiArtifactBucketObjects
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                  - s3:GetObjectVersionAcl
                Resource:
                  - !Sub 'arn:aws:s3:::${ExportsBucket}/${ConfObjDumpS3Url}*'
                  - !Sub 'arn:aws:s3:::${ExportsBucket}/${TimelineForInvoicingZipS3Url}*'
              - Sid: CiArtifactBucketWriteResultsObjects
                Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:PutObjectVersion
                  - s3:PutObjectVersionAcl
                Resource:
                  - !Sub 'arn:aws:s3:::${ExportsBucket}/${TimelineForInvoicingRedonePath}/*'


